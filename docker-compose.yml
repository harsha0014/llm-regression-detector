version: '3.8'
services:
  eval:
    build: .
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL}
    volumes:
      - ./eval_runs:/app/eval_runs
      - ./prompts:/app/prompts